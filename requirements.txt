# 视频帧高度修改工具的依赖库

# GUI框架 - tkinter是Python标准库，无需安装

# 注意：本项目主要使用Python标准库
# 如果您的Python环境中缺少某些模块，请根据错误信息安装对应的包

# 可选的增强库（如果需要更好的GUI体验）：
# pillow>=10.0.0  # 用于图像处理和图标显示
# tkinterdnd2>=0.3.0  # 用于拖拽功能（可选）

# 系统要求：
# - Python 3.7+
# - Windows 操作系统
# - FFmpeg 8.0+ (已包含在bin目录中)

# 开发依赖（可选）：
# pytest>=7.0.0  # 用于单元测试
# black>=22.0.0   # 代码格式化
# flake8>=5.0.0   # 代码检查